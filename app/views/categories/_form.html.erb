<div class="c-container">
<%= form_with(model: category, url: user_categories_path(current_user)) do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field-container">
  <div class="header">
   <%= link_to "Back", user_categories_path(current_user), class: "btn" %>
  <h3 class="title">Add new Category</h3>
  <div>
     <%= form.submit "Add Category", class: "btn" %>
  </div>
   </div>

  <div class="field">
  <%= form.text_field :name, autofocus: true, autocomplete: "name", class: "input", placeholder: "Name" %>
  </div>

  <div class="field">
  <%= form.text_field :icon, autofocus: true, autocomplete: "name", class: "input", placeholder: "URL" %>
  </div>

 
<% end %>
</div>
