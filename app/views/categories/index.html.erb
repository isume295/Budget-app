
<div class="c-c">
<div class="c-container">
 <div class="header">
  <%= link_to "New", new_user_category_path, class: "btn" %>
  <h3 class="title">CATEGORY</h3>
  <div class="sign-out">
  <%= link_to 'Sign Out', destroy_user_session_path, data: { turbo_method: :delete }, class: "btn" %>
  </div>
  </div>

<div id="categories">
 
  <% if @categories.empty? %>
      <p>No categories found.</p>
    <% else %>
      <% @categories.each do |category| %>
        <%= render category %>
      <% end %>
    <% end %>
</div>
</div>
<div class="new">
<%= link_to "New category", new_user_category_path, class: "new-data" %>
</div>
</div>
<p style="color: green"><%= notice %></p>